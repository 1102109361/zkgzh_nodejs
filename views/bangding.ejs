<!DOCTYPE html>
<html lang="zh">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, user-scalable=no, initial-scale=1.0, maximum-scale=1.0, minimum-scale=1.0">
    <title>公众号绑定</title>
    <style>
        .page-bg{
            box-sizing: border-box;
            width: 100%;
            padding: 0 38px;
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
        }

        .page-title{
            color: #333333;
            font-size: 24px;
            font-weight: bold;
            margin-top: 50px;
        }

        .page-icon{
            width: 194px;
            height: 49px;
            margin-top: 32px;
        }

        .confirm-info{
            width: 100%;
            color: #333333;
            font-size: 16px;
            margin-top: 45px;
            padding-bottom: 16px;
            border-bottom: 1px solid #EEEEEE;
        }

        .confirm-tips{
            color: #999999;
            margin-top: 16px;
        }

        .confirm-tips-title{
            font-size: 16px;
            font-weight: bold;
        }

        .confirm-tips-content{
            display: flex;
            font-size: 14px;
            margin-top: 10px;
        }

        .gray-point{
            flex-shrink: 0;
            display: inline-block;
            width: 8px;
            height: 8px;
            border-radius: 50%;
            background: rgba(153, 153, 153, 0.39);
            margin-top: 8px;
            margin-right: 8px;
        }

        .tips-content-text{
            display: inline-block;
        }

        .confirm-btn{
            display: flex;
            justify-content: center;
            align-items: center;
            width: 100%;
            height: 40px;
            color: #fff;
            font-size: 14px;
            background: #1890FF;
            border-radius: 2px;
            margin-top: 54px;
        }

        .tick-icon{
            width: 80px;
            height: 80px;
            margin-top: 80px;
        }

        .result-text{
            color: #333;
            font-size: 24px;
            margin-top: 20px;
        }
        .lightbtn {
            display: flex;
            justify-content: center;
            align-items: center;
            width: 100%;
            height: 40px;
            color: #fff;
            font-size: 14px;
            background: #1890FF;
            border-radius: 2px;
            margin-top: 54px;
        }
        .lightbtn button{
            width:auto;
            height:auto;
            padding:5px 10px;
            box-shadow: none;
        }
        .lightbtn .cancelBtn {
            background-color: rgb(255, 255, 255);
            border: 1px solid rgba(0, 0, 0, .5);
        }
        .lightbtn>form{
            width: 100%;
            height: 40px;
        }
        .lightbtn .sureBtn {
            width: 100%;
            height: 40px;
            color: #fff;
            border:1px solid #1890FF;
            background-color: #1890FF;
        }
        .defaultBtn{
            background-color: rgb(255, 255, 255);
            border: 1px solid rgba(0, 0, 0, .5);
        }
    </style>
</head>
<body>
<div class="page-bg">
    <% if(type==1){%>
        <!-- 绑定公众号 -->
        <div class="page-title">公众号绑定</div>
            <image src="/accountConIcon.png" class="page-icon"></image>
            <div class="confirm-info">
                您确认<%=siteWebName%>账号（账号：<%=hideIdCard%>）绑定此公众号吗？
            </div>
            <div class="confirm-tips">
                <div class="confirm-tips-title">绑定成功后可享受以下权益</div>
                <div class="confirm-tips-content">
                    <div class="gray-point"></div>
                    <div class="tips-content-text">
                        可接收到<%=siteWebName%>所推送通知信息（如：审核通知、考试通知、成绩查询通知等）。
                    </div>
                </div>
            </div>
            <div class="lightbtn">
                <form name="reply"  method="post" action="toBind">
                    <input type="text" value="<%=k%>" name="k" style="display: none;">
                    <!-- <button class="defaultBtn" onclick="closePage()" type="button">返回</button> -->
                    <button class="sureBtn" type="submit">立即绑定</button>
                </form>
            </div>
        </div>
    <%} else if(type==2){%>
        <!-- 绑定成功 -->
        <image class="tick-icon" src="/tick-icon.png"></image>
        <div class="result-text">绑定成功</div>
        <div class="lightbtn" style="margin-top:58px">
            <form name="reply"  method="get" action="bindUser">
                <input type="text" value="<%=k%>" name="k" style="display: none;">
                <!-- <button class="defaultBtn" onclick="closePage()" type="button">返回</button> -->
                <button class="sureBtn" type="submit">确定</button>
            </form>
        </div>
    <%}else if(type==3){%>
        <!-- 已绑定可解绑 -->
        <div class="page-title">公众号解绑</div>
            <image src="/accountConIcon.png" class="page-icon"></image>
            <div class="confirm-info">
                您确认<%=siteWebName%>账号（账号：<%=hideIdCard%>）解除绑定此公众号吗？
            </div>
            <div class="confirm-tips">
                <div class="confirm-tips-content">
                    <div class="gray-point"></div>
                    <div class="tips-content-text">
                        解除绑定成功后，公众号将无法接收<%=siteWebName%>所推送通知信息（如：审核通知、考试通知、成绩查询通知等）。
                    </div>
                </div>
            </div>
            <div class="lightbtn">
                <form name="reply"  method="post" action="unBind">
                    <input type="text" value="<%=k%>" name="k" style="display: none;">
                    <!-- <button class="cancelBtn" onclick="hideUnBind()" type="button">取消</button> -->
                    <button class="sureBtn" type="submit">立即解绑</button>
                </form>
            </div>
        </div>
    <%} else if(type==4){%>
        <!-- 解除绑定成功 -->
        <image class="tick-icon" src="/tick-icon.png"></image>
        <div class="result-text">解除绑成功</div>
        <div class="lightbtn" style="margin-top:58px">
            <form name="reply"  method="get" action="bindUser">
                <input type="text" value="<%=k%>" name="k" style="display: none;">
                <!-- <button class="defaultBtn" onclick="closePage()" type="button">返回</button> -->
                <button class="sureBtn" type="submit">确定</button>
            </form>
        </div>
    <%}%>
</div>
<!-- <div id="app">
        <div class="icon">
            <img src="../zplogo.png" alt="">
        </div>
        <% if(type==1){%>
            <div style="text-align: center;">
                <span style="font-size: 22px;">绑定确认</span></span>
            </div>
            <div class="smallSize" style="line-height: 22px;margin: 10px">
                请确认是否将当前微信号 与 福建省事业单位公开招聘考试报名平台 以下账号进行绑定，绑定后可随时获取审核动态通知、考试通知、成绩查询等。
            </div>
            <div class="smallSize">
                <div>姓名:<span><%=name%></span></div>
                <div>身份证号:<span><%=hideIdCard%></span></div>
            </div>
            <div style="text-align: center;margin-top: 40px;">
                <form name="reply"  method="post" action="toBind">
                    <input type="text" value="<%=k%>" name="k" style="display: none;">
                    <button class="defaultBtn" onclick="closePage()" type="button">返回</button>
                    <button class="bindBtn" type="submit">立即绑定</button>
                </form>
            </div>
        <%} else if(type==2){%>
            <div class="" style="text-align: center;">
                <span style="font-size: 22px;">绑定成功！</span>
            </div>
            <div style="text-align: center;margin-top: 40px;">
                <form name="reply"  method="get" action="bindUser">
                    <input type="text" value="<%=k%>" name="k" style="display: none;">
                    <button class="defaultBtn" onclick="closePage()" type="button">返回</button>
                    <button class="bindBtn" type="submit">确定</button>
                </form>
            </div>
        <%}else if(type==3){%>
            <div style="text-align: center;">
                <span style="font-size: 22px;">当前账号已绑定以下账号</span>
            </div>
            <div class="smallSize">
                <div>姓名:<span><%=name%></span></div>
                <div>身份证号:<span><%=hideIdCard%></span></div>
            </div>
            <div class="smallSize" style="line-height: 22px;margin: 10px">
                若需要更换绑定账号，点击解绑后，重新绑定
            </div>
            <div style="text-align: center;margin-top: 70px;">
                <button class="defaultBtn" onclick="closePage()" type="button">返回</button>
                <button class="unBindBtn" onclick="sureUnBind()">立即解绑</button>
            </div>
            <div id="darkBox">
                <div class="light">
                    <div style="margin: 10px 10px 0 10px; font-size: 16px;">正在解绑</div>
                    <div style="margin: 10px;">解绑后，你将无法通过福建招聘平台实时获取审核动态、考试通知，是否确认解除绑定关系?</div>
                    <div class="lightbtn">
                        <form name="reply"  method="post" action="unBind">
                            <input type="text" value="<%=k%>" name="k" style="display: none;">
                            <button class="cancelBtn" onclick="hideUnBind()" type="button">取消</button>
                            <button class="sureBtn" type="submit">继续操作</button>
                        </form>
                    </div>
                </div>
            </div>
        <%}%>
    </div> -->
</body>

</html>

<script>
    function closePage(){
        if (window.WeixinJSBridge) {
            document.addEventListener('WeixinJSBridgeReady',function () {
                window.WeixinJSBridge.call('closeWindow');
            },false);
            window.WeixinJSBridge.call('closeWindow');
        } else {
            window.close();
        }
    }
    function sureUnBind(){
        document.getElementById('darkBox').style.display = 'block';
    }
    function hideUnBind(){
        document.getElementById('darkBox').style.display = 'none';
    }
</script>

<!-- <style>
    body {
        background-color: #ffffff;
        letter-spacing: 0.2px;
    }

    .icon {
        margin: 100px 0 30px 0;
        text-align: center;
    }

    .icon img {
        width: 60px;
        height: 60px;
    }

    .smallSize {
        font-size: 15px;
        margin: 20px 10px;
    }
    .smallSize span{
        margin-left:10px;
    }
    button{
        width: 140px;
        height: 40px;
        border-radius: 5px;
    }
    .bindBtn {
        color:#fff;
        border: 0 solid #ffffff00;
        background-color: #51c75b;
    }
    .unBindBtn{
        color:#fff;
        border: 0 solid #ffffff00;
        background-color: #f5a300;
    }

    #darkBox {
        width: 100%;
        height: 100%;
        position: fixed;
        left: 0;
        top: 0;
        z-index: 9;
        background: rgba(0, 0, 0, .5);
        display: none;
    }

    .light {
        display: flex;
        width: 280px;
        /* height: 150px; */
        padding-bottom:10px;
        border-radius: 5px;
        border: 1px solid rgba(253, 251, 251, 0);
        position: fixed;
        top: 50%;
        left: 50%;
        transform: translate(-50%, -50%);
        background-color: #fff;
        flex-direction: column;
    }

    .lightbtn {
        text-align: center;
        margin-top: 10px;
    }
    .lightbtn button{
        width:auto;
        height:auto;
        padding:5px 10px;
        box-shadow: none;
    }
    .lightbtn .cancelBtn {
        background-color: rgb(255, 255, 255);
        border: 1px solid rgba(0, 0, 0, .5);
    }

    .lightbtn .sureBtn {
        color: #fff;
        margin-left:10px;
        border:1px solid rgb(62 139 248);
        background-color: rgb(62 139 248);
    }
    .defaultBtn{
        background-color: rgb(255, 255, 255);
        border: 1px solid rgba(0, 0, 0, .5);
    }
</style> -->
